{% extends 'base.html.twig' %}

{% block title %}
    Food Ordering App
{% endblock %}

{% block body %}
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="{{ path("foods") }}">Food Ordering</a>
            </div>
            <ul class="nav navbar-nav">
        {% if not loggedIn %}
            <li><a href="{{ path('login') }}"> Log in</a></li>
            <li><a href="{{ path('register') }}"> Register </a></li>
        {% else %}
            {% if not admin %}
                <li><a href="{{ path('address_mod') }}"> Change My Address</a></li>
                <li><a href="{{ path('cart') }}"> View Cart</a></li>
            {% else %}
                <li><a href="{{ path('add_admin') }}"> Add Administrator</a></li>

            {% endif %}
            <li><a href="{{ path('foods') }}"> Add Items to Cart</a></li>
            <li><a href="{{ path('account') }}"> Account settings</a></li>
            <li><a href="{{ path('logout') }}"> Logout</a></li>
        {% endif %}
            </ul>
        </div>
    </nav>
    {% for flash_message in app.session.flashbag.get('notice') %}
        <div class="alert alert-info">
            {{ flash_message }}
        </div>
    {% endfor %}

    {% block orderbody %}{% endblock %}

    <hr />
{% endblock %}